#!/bin/sh

REQUIRE_OPENMP=true
COMMON_FILES="${PCVS_CSOURCE_DIR}/../common/common.c"
CFLAGS="${CFLAGS} -fopenmp ${PRIVATIZATION} -DOMPVER2 -DOMPVER3 -I${PCVS_CSOURCE_DIR}/../common/ -lm"
MPC_OPENMP_THREAD_LIST="1 2 4 8"
MPC_PROC_NB_LIST=1
MPC_TASK_NB_LIST=1
MPC_THREAD_NB_LIST=1

openmp_configuration
