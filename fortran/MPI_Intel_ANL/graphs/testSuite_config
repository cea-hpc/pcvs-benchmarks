#!/bin/sh

COMMON_FILES="${PCVS_CSOURCE_DIR}/../common/libmpitestf.F ${PCVS_CSOURCE_DIR}/../common/reduce/reduce.F"
CFLAGS="${CFLAGS} -I${PCVS_CSOURCE_DIR}/../common/"

MPC_TASK_NB_LIST="6 32"
multiple_configuration
