#!/bin/bash

part="$1"
shift 1 
if test "$part" != "local"; then 
ccc_mprun -v -K -p $part -x ./compil_parallel local $@
else
echo $SLURM_JOB_CPUS_PER_NODE
ccc_mprun -v -c $SLURM_JOB_CPUS_PER_NODE -p $part -x $@
fi
