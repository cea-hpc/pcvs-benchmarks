#!/bin/sh

COMMON_FILES="${PCVS_CSOURCE_DIR}/../common/libmpitest.c ${PCVS_CSOURCE_DIR}/../common/reduce.c"
CFLAGS="${CFLAGS} -I${PCVS_CSOURCE_DIR}/../common/"
MPC_TASK_NB_LIST="6 16"
multiple_configuration
